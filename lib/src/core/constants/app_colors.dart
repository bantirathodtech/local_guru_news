import 'package:flutter/material.dart';

class AppColors {
  // 🌿 Primary brand colors
  static const Color primary = Colors.white;
  // Color(0xFF00695C); // Teal Green (trust, healthcare)
  static const Color primaryDark = Color(0xFF004D40); // Dark Teal
  static const Color accent =
      Color(0xFF29B6F6); // Sky Blue (fresh, approachable)

  // 🧼 Backgrounds and surfaces
  static const Color background =
      Color(0xFFF9FAFB); // Very light grey (clean hospital feel)
  static const Color card = Color(0xFFFFFFFF); // Pure white for cards

  // 📝 Text colors
  static const Color textPrimary = Color(0xFF0D1B2A); // Deep Navy (readability)
  static const Color textSecondary =
      Color(0xFF4F5B62); // Cool Grey (subtle text)
  static const Color textTertiary = Colors.white; // White (for dark/primary bg)

  // 🚨 Status colors
  static const Color error = Color(0xFFD32F2F); // Medical Alert Red
  static const Color success = Color(0xFF2E7D32); // Healing Green
  static const Color warning = Color(0xFFF9A825); // Warm Amber (alerts)

  // 🧩 Utility
  static const Color grey =
      Color(0xFFB0BEC5); // Neutral Cool Grey (dividers, borders)
  static const Color lightGrey = Color(0xFFF1F3F4); // Light hospital section bg
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}
