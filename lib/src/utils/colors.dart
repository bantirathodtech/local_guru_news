import 'package:flutter/material.dart';

const primaryColor = const Color(0xFFffffff);

const tempColor = const Color(0xFFcccccc);

const disabledColor = Colors.grey;

const enabledColor = Colors.redAccent;
